{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "exclude": ["frontend/**", "*.json", "*.md", "paper/**", ".gitignore"]
    }
  },
  "deploy": {
    "startCommand": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}